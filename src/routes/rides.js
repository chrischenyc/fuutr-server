const express = require('express');

const router = express.Router();

/**
 * Authenticated routes to manage user's rides
 */

// TODO: /rides GET - list history rides

// TODO: /rides POST - create a new ride

// TODO: /rides/{id} GET - retrieve detail of a ride

// TODO: /rides/{id} PATCH - update a ride during riding

module.exports = router;
